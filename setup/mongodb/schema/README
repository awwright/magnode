Properties can go in this order:

_id (an ObjectId)
id (a URI)
label
description (optional)
collection (where to retrieve an instance)
put (how to store an instance)
menu
$schema (always "http://json-schema.org/draft-04/schema#")
type (always "object")
links
properties
